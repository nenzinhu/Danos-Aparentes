import AppLoadingShell from '@/src/components/app/AppLoadingShell'

/** Instant Suspense fallback so Entrar updates the UI immediately on tap. */
export default function AppLoading() {
  return <AppLoadingShell />
}
